server:
  port: 8190

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mileage_review_query
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

axon:
  serializer:
    general: jackson
  axonserver:
    servers: localhost:8124

logging:
  level:
    com:
      mileage: debug
